'use strict'

/**
 * The preparation steps for updating the application software. Used with
 * updateSoftwareDialog.
 *
 * The information herein complements or duplicates that in
 * README_updating.md.
 */

const RQUpdateHelp = {}

RQUpdateHelp.version = 1
RQUpdateHelp.steps = []
RQUpdateHelp.steps.push('Ensure the connection to wall power')
RQUpdateHelp.steps.push('Set the battery charger to ON')
RQUpdateHelp.steps.push('Keep the Internet connection stable')
RQUpdateHelp.steps.push('Change to screen 4 on the HAT')
RQUpdateHelp.steps.push('Confirm it shows "HAT setup"')
RQUpdateHelp.steps.push('Click the Update button below')
RQUpdateHelp.steps.push('WAITING FOR ROBOT will appear')
RQUpdateHelp.steps.push('Time to update is between 5 and 30 minutes')
RQUpdateHelp.steps.push('When screen 4 shows "HAT setup" again, shutdown the robot')
RQUpdateHelp.steps.push('Close the browser or browser tab')
